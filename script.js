console.log("Gatto Bot Dashboard loaded!");

const loginButton =
    document.querySelector(".discord-login");

loginButton.addEventListener("click", () => {

    alert(
        "Discord login will be connected next!"
    );

});