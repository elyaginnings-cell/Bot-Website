{
  "name": "discord-bot-dashboard",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "echo 'Static + serverless — no frontend build step required'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.16.3"
  }
}
