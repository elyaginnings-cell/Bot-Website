{
  "name": "discord-bot-dashboard",
  "version": "1.2.5",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "mkdir -p public && cp -f *.html *.css *.js *.svg *.txt public/ 2>/dev/null || true"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.16.3"
  }
}
